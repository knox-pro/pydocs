# 多标签图像分类与实战

## 什么是多标签图像分类

  * 现实生活中的图像往往包含多个种类目标，多标签分类需要对多个目标进行独立分类识别。


![img_24.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_24.png)

> 标签为Nx1的向量:[1,1,0...1,0]

## 多标签分类常见评估指标

  * 平均准确率:当所有标签均预测正确时，准确率才为1


![img_25.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_25.png)

  * 汉明损失:将预测的标签集合与实际的标签集合进行对比，按照汉明距离来衡量


![img_26.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_26.png)

## 多标签分类模型

### N个二分类模型

  * 对每一个维度进行单独预测，N表示样本数量，K表示标签类别维度，c表示每一个维度的二分类
  * 各个标签类别之间没有竞争关系


![img_27.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_27.png)

![img_28.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_28.png)

  * 实现简单，但是没有考虑各个类别之间的关系
  * '倒影’-> '天空’ 同时出现概率大。


### 标记关系建模CNN-RNN

  * 将多标签分类建模成序列化预测过程，充分考虑了类别之间的相关性
  * 船>海洋，采用迭代的方式进行多个标签的预测先预测ship，再预测sea.


![img_29.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_29.png)

  * 最终按照一定的顺序对各个维度的标签进行预测


![img_30.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_30.png)

## 生活用品多标签分类实战

### 数据集介绍： PASCAL数据集

  * 来源于2005到2012年的PASCALVisual Object Classes项目、


![img_31.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_31.png)

### 分类任务相关的文件目录

  * JPEGImages是所有图片
  * Annotations是对应的标注文件
  * ImageSets是各任务的数据集划分


![img_32.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_32.png)

### 图片和标签文件夹

![img_33.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_33.png)

> 命名格式为year %6d，其中year表示该图片被加入到数据集的年份，%6d表示长度为6位的image index.

### 标注格式

  * 在ImageSets/Main文件夹下包含了数据集划分与类别属性
  * train.txt,trainval.txt,val.txt即训练集与验证集划分
  * 每个文档的各行标签数值为1或者-1，1表示图片中存在该类别的物体，-1则表示图片中不存在该类别的物体。


![img_34.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_34.png)

### 数据读取

#### 构建标签

  * 步骤1:读取图片列表
  * 步骤2:读取对应的xml文件
  * 步骤3:根据xml文件中的项，判定是否存在该类别
  * 步骤4:记录所有类别结果并存储


![img_35.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_35.png)

#### 标签生成与数据读取python代码

> multi_label目录下代码

  * dataset.py


    
    
    import os
    import sys
    sys.path.append(os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
    import argparse
    import numpy as np
    
    IMG_FOLDER_NAME = "JPEGImages" # 图片文件夹
    ANNOT_FOLDER_NAME = "Annotations" # 标签文件夹
    IGNORE = 255
    
    # 标签字符串名称
    CAT_LIST = ['aeroplane', 'bicycle', 'bird', 'boat',
            'bottle', 'bus', 'car', 'cat', 'chair',
            'cow', 'diningtable', 'dog', 'horse',
            'motorbike', 'person', 'pottedplant',
            'sheep', 'sofa', 'train',
            'tvmonitor']
    
    N_CAT = len(CAT_LIST)
    
    # 标签转换为数字
    CAT_NAME_TO_NUM = dict(zip(CAT_LIST,range(len(CAT_LIST))))
    
    def load_img_name_list(dataset_path):
        img_name_list = np.loadtxt(dataset_path,dtype=str)
        return img_name_list
    
    def get_img_path(img_name, voc12_root):
        if not isinstance(img_name, str):
            img_name = decode_int_filename(img_name)
        return os.path.join(voc12_root, IMG_FOLDER_NAME, img_name + '.jpg')
    
    def load_image_label_list_from_xml(img_name_list, voc12_root):
        return [load_image_label_from_xml(img_name, voc12_root) for img_name in img_name_list]
    
    # 单张图片的标签构建
    def load_image_label_from_xml(img_name, voc12_root):
        from xml.dom import minidom
        # 获取xml文件中<>里的内容 --> 生成对应的列表
        elem_list = minidom.parse(os.path.join(voc12_root, ANNOT_FOLDER_NAME, decode_int_filename(img_name) + '.xml')).getElementsByTagName('name')
        # 构建一个空矩阵
        multi_cls_lab = np.zeros((N_CAT), np.float32) 
        # 在列表内循环，并完成对label矩阵的修改
        for elem in elem_list:
            cat_name = elem.firstChild.data
            if cat_name in CAT_LIST:
                cat_num = CAT_NAME_TO_NUM[cat_name] # 判断该类别是否存在于现有的类别中
                multi_cls_lab[cat_num] = 1.0 # 若对应，则将相应的位置置为1
    
        return multi_cls_lab
    
    def decode_int_filename(int_filename):
        s = str(int(int_filename))
        return s[:4] + '_' + s[4:]
    
    def load_image_label_list_from_npy(cls_labels_dict,img_name_list):
        return np.array([cls_labels_dict[img_name] for img_name in img_name_list])
    
    if __name__ == '__main__':
        parser = argparse.ArgumentParser()
        parser.add_argument("--train_list", default='VOC2012/ImageSets/Main/train.txt', type=str)
        parser.add_argument("--val_list", default='VOC2012/ImageSets/Main/val.txt', type=str)
        parser.add_argument("--out", default="cls_labels.npy", type=str)
        parser.add_argument("--voc12_root", default="VOC2012", type=str)
        args = parser.parse_args()
    
        # 获取训练列表和验证列表
        train_name_list = load_img_name_list(args.train_list)
        val_name_list = load_img_name_list(args.val_list)
    
        # 二者融合
        train_val_name_list = np.concatenate([train_name_list, val_name_list], axis=0)
    
        # 获取label信息
        label_list = load_image_label_list_from_xml(train_val_name_list, args.voc12_root)
    
        # 先构建一个空的multi hot矩阵，初始值均为零
        total_label = np.zeros(20)
    
        # 构建一个字典
        d = dict()
        # 循环读取标签文件，并完成对label矩阵的修改 --> 实现了name和label的一一匹配关系
        for img_name, label in zip(train_val_name_list, label_list):
            d[img_name] = label
            total_label += label
    
        # 打印结果
        print(total_label)
        # 存储
        np.save(args.out, d)
    
        cls_labels_dict = np.load(args.out, allow_pickle=True).item()
        label = load_image_label_list_from_npy(cls_labels_dict,train_name_list)
        print('label',label)
    

  * getlabel.py


    
    
    import torch
    import torchvision
    import torchvision.datasets as datasets
    import torch.utils.data as data
    import torchvision.transforms as transforms
    import cv2
    from PIL import Image
    import argparse
    import numpy as np
    import os
    from getlabel import load_img_name_list,load_image_label_list_from_npy 
    
    # 定义数据类
    class VOCDataset(data.Dataset):
        def __init__(self,datalist,labellist,data_root,data_transform):
            self.samples = []
            self.transforms = data_transform
            for img_name, label in zip(datalist, labellist):
                imagepath = os.path.join(data_root,'JPEGImages',img_name+'.jpg')
                self.samples.append((imagepath,label))
    
        def __getitem__(self,index):
            imagepath,label = self.samples[index]
            img = Image.open(imagepath)
            return self.transforms(img),label
    
        def __len__(self):
            return len(self.samples)
    
    
    if __name__ == '__main__':
        parser = argparse.ArgumentParser()
        parser.add_argument("--train_list", default='VOC2012/ImageSets/Main/train.txt', type=str)
        parser.add_argument("--voc12_root", default="VOC2012", type=str)
        args = parser.parse_args()
        # 数据预处理方法
        data_transform = transforms.Compose([
                transforms.Resize(256),
                transforms.RandomResizedCrop(224),
                transforms.ToTensor(),
                transforms.Normalize([0.5,0.5,0.5], [0.5,0.5,0.5])
            ])
    
        # 读取文件
        cls_labels_dict = np.load("cls_labels.npy", allow_pickle=True).item()
        datalist = load_img_name_list(args.train_list)
        labellist = load_image_label_list_from_npy(cls_labels_dict,datalist)
        mydata = VOCDataset(datalist=datalist,labellist=labellist,data_root=args.voc12_root,data_transform=data_transform) ## 自己定义的数据类
    
        print('nums=',mydata.__len__())
        image0,label0=mydata.__getitem__(0)
        print('sample=',mydata.__getitem__(0))
        print(type(label0))
    
        # 多标签损失优化目标
        import torch.nn.functional as F
        loss = F.multilabel_soft_margin_loss(torch.rand((1,20)),torch.from_numpy(label0))
        print('loss=',loss)
    

### 模型搭建与训练

### 标准分类模型+多标签分类优化目标
    
    
    等价的优化目标定义形式
    torch.nn.BCEWithLogitsLoss
    torch.nn.MultiLabelSoftMarginLoss
    sigmoid + torch.nn.BCELoss()
    torch.nn.functional.multilabel_soft_margin_loss
    
    使用方法 multilabel_soft_margin_loss(x,y)
    x是模型预测结果，其shape是(N,C)，N为batch size，C是分类数;y是真实标签，shape也是(N,C)
    

  * net.py


    
    
    import torch
    import torch.nn as nn
    
    # 残差模块
    class ResidualBlock(nn.Module):
        def __init__(self, in_channels, outchannels):
            super(ResidualBlock, self).__init__()
            self.channel_equal_flag = True
            if in_channels == outchannels:
                self.conv1 = nn.Conv2d(in_channels=in_channels, out_channels=outchannels, kernel_size=3, padding=1, stride=1)
            else:
                ## 对恒等映射分支的变换，当通道数发生变换时，分辨率变为原来的二分之一
                self.conv1x1 = nn.Conv2d(in_channels=in_channels, out_channels=outchannels, kernel_size=1, stride=2)
                self.bn1x1 = nn.BatchNorm2d(num_features=outchannels)
                self.channel_equal_flag = False
    
                self.conv1 = nn.Conv2d(in_channels=in_channels, out_channels=outchannels,kernel_size=3,padding=1, stride=2)
    
            self.bn1 = nn.BatchNorm2d(num_features=outchannels)
            self.relu = nn.ReLU(inplace=True)
    
            self.conv2 = nn.Conv2d(in_channels=outchannels, out_channels=outchannels, kernel_size=3,padding=1)
            self.bn2 = nn.BatchNorm2d(num_features=outchannels)
    
        def forward(self, x):
            identity = x
            x = self.conv1(x)
            x = self.bn1(x)
            x = self.relu(x)
    
            x = self.conv2(x)
            x = self.bn2(x)
            x = self.relu(x)
    
            if self.channel_equal_flag == True:
                pass
            else:
                identity = self.conv1x1(identity)
                identity = self.bn1x1(identity)
                identity = self.relu(identity)
    
            out = identity + x
            return out
    
    class ResNet18(nn.Module):
        def __init__(self, num_classes=1000):
            super(ResNet18, self).__init__()
            # conv1
            self.conv1 = nn.Conv2d(in_channels=3, out_channels=64,kernel_size=7,stride=2, padding=3)
            self.bn1 = nn.BatchNorm2d(num_features=64)
            self.relu = nn.ReLU(inplace=True)
    
            # conv2_x
            self.maxpool = nn.MaxPool2d(kernel_size=3, stride=2,padding=1)
            self.conv2_1 = ResidualBlock(in_channels=64, outchannels=64)
            self.conv2_2 = ResidualBlock(in_channels=64, outchannels=64)
    
            # conv3_x
            self.conv3_1 = ResidualBlock(in_channels=64, outchannels=128)
            self.conv3_2 = ResidualBlock(in_channels=128, outchannels=128)
    
            # conv4_x
            self.conv4_1 = ResidualBlock(in_channels=128, outchannels=256)
            self.conv4_2 = ResidualBlock(in_channels=256, outchannels=256)
    
            # conv5_x
            self.conv5_1 = ResidualBlock(in_channels=256, outchannels=512)
            self.conv5_2 = ResidualBlock(in_channels=512, outchannels=512)
    
            # avg_pool
            self.avg_pool = nn.AdaptiveAvgPool2d(output_size=(1,1)) # [N, C, H, W] = [N, 512, 1, 1]
    
            # fc
            self.fc = nn.Linear(in_features=512, out_features=num_classes)  # [N, num_classes]
    
        def forward(self, x):
            # conv1
            x = self.conv1(x)
            x = self.bn1(x)
            x = self.relu(x)
    
            # conv2_x
            x = self.maxpool(x)
            x = self.conv2_1(x)
            x = self.conv2_2(x)
    
            # conv3_x
            x = self.conv3_1(x)
            x = self.conv3_2(x)
    
            # conv4_x
            x = self.conv4_1(x)
            x = self.conv4_2(x)
    
            # conv5_x
            x = self.conv5_1(x)
            x = self.conv5_2(x)
    
            # avgpool + fc + softmax
            x = self.avg_pool(x)
            x = torch.flatten(x, 1)
            x = self.fc(x)
    
            return x
    
    if __name__ == '__main__':
        model = ResNet18(num_classes=1000)
        input = torch.randn([1, 3, 224, 224])
        output = model(input)
    
        torch.save(model, 'resnet18.pth')
        torch.onnx.export(model,input,'resnet18.onnx')
    

  * train.py


    
    
    from __future__ import print_function, division
    
    import torch
    import torch.optim as optim
    from torch.optim import lr_scheduler
    from torchvision import datasets, models, transforms
    from dataset import VOCDataset
    from net import ResNet18
    import numpy as np
    import os
    import torch.nn.functional as F
    from getlabel import load_img_name_list,load_image_label_list_from_npy
    import matplotlib.pyplot as plt
    ## 使用tensorboard接口进行可视化
    from torch.utils.tensorboard import SummaryWriter
    writer = SummaryWriter('logs') ## 创建一个SummaryWriter的示例，默认目录名字为runs
    
    # 每个类别的精度计算
    def EvaluateClass(gts, clc_results):
        # gts:N*20标签向量
        # clc_results:N*20结果向量
        average_precisions = [] # 存储所有的平均精度指标
        # 对每一个类单独统计精度与召回率指标
        for i in range(0,gts.shape[1]):
            gt = gts[:,i] # 取所有样本的真值
            clc_result = clc_results[:,i] # 取所有样本的预测值
            # 将当前某一个类的概率预测结果进行降序排列，以便后续计算topN指标
            sorted_index = np.argsort(clc_result)[::-1]
            # 相应评价指标获取
            true_positives = gt[sorted_index] == 1  # 统计真的正样本数量，即真阳率
            false_positives = gt[sorted_index] == 0  # 统计假的正样本数量，即假阳率
            true_positives = np.cumsum(true_positives)  # 将真阳率数组按行累加，以便计算召回率和精度
            false_positives = np.cumsum(false_positives) # 将假阳率数组按行累加，以便计算召回率和精度
            recalls = true_positives / np.sum(gt > 0)  # 计算召回率
            eps = 1e-10
            positives = false_positives + true_positives
            precisions = true_positives / (positives + (positives == 0.0) * eps) # 计算精度
    
            # 根据召回率，计算平均精度（不同召回率下最大精度的平均值），即mAP
            average_precision = 0;
            for threshold in np.arange(0, 1.1, 0.1):
                precisions_at_recall_threshold = precisions[recalls >= threshold]
                if precisions_at_recall_threshold.size > 0:
                    max_precision = np.max(precisions_at_recall_threshold)
                else:
                    max_precision = 0
                average_precision = average_precision + max_precision / 11;
            average_precisions.append(average_precision) # 记录各个类别的精度
    
        return average_precisions
    
    ## 训练主函数
    def train_model(model, optimizer, scheduler, num_epochs=25):
        train_accs = [] # 训练精度变量
        val_accs = [] # 验证精度变量
        for epoch in range(num_epochs):
            print('Epoch {}/{}'.format(epoch, num_epochs - 1))
            for phase in ['train', 'val']:
                gts = []
                clc_results = []
                if phase == 'train':
                    scheduler.step()
                    model.train(True)  ## 设置为训练模式
                else:
                    model.train(False)  ## 设置为验证模式
    
                running_loss = 0.0 ##损失变量
                number_batch = 0 ##
    
                ## 从dataloaders中获得数据
                for data in dataloaders[phase]:
                    inputs, labels = data 
                    if use_gpu:
                        inputs = inputs.cuda()
                        labels = labels.cuda()
    
                    optimizer.zero_grad() ## 清空梯度
                    outputs = model(inputs) ## 前向运行
                    loss = F.multilabel_soft_margin_loss(outputs, labels) ## 计算损失
    
                    for i in range(0,labels.shape[0]):
                        gts.append(labels[i].cpu().detach().numpy())  ## 存储每一个样本的真值
                        clc_results.append(outputs[i].cpu().detach().numpy()) ## 存储每一个样本的预测结果
    
                    if phase == 'train':
                        loss.backward() ##误差反向传播
                        optimizer.step() ##参数更新
    
                    running_loss += loss.data.item()
                    number_batch += 1
    
                ## 得到每一个epoch的平均损失与精度
                epoch_loss = running_loss / number_batch
                gts = np.array(gts)
                clc_results = np.array(clc_results)
                accs = EvaluateClass(gts,clc_results) # 计算各个类别的精度
                epoch_acc = sum(accs) / len(accs) # 计算所有类别的平均精度
    
                ## 收集精度和损失用于可视化
                if phase == 'train':
                    writer.add_scalar('data/trainloss', epoch_loss, epoch)
                    writer.add_scalar('data/trainacc', epoch_acc, epoch)
                    train_accs.append(epoch_acc)
                else:
                    writer.add_scalar('data/valloss', epoch_loss, epoch)
                    writer.add_scalar('data/valacc', epoch_acc, epoch)
                    val_accs.append(epoch_acc)
    
                ## 收集精度和损失用于可视化
                print('{} Loss: {:.4f} Acc: {:.4f}'.format(phase, epoch_loss, epoch_acc))
    
        writer.close()
    
        # 绘制精度曲线
        plt.figure()
        np.savetxt(r'train_acc.txt',train_accs)
        np.savetxt(r'val_acc.txt',val_accs)
        plt.plot(train_accs,label='train_acc')
        plt.plot(val_accs,label='val_acc')
        plt.legend()
        plt.savefig('acc.jpg')
        return model
    
    if __name__ == '__main__':
        image_size = 256 ##图像统一缩放大小
        crop_size = 224 ##图像裁剪大小，即训练输入大小
        nclass = 20 ##分类类别数
        model = ResNet18(num_classes=nclass) ##创建模型
    
        ## 模型缓存接口
        if not os.path.exists('models'):
            os.mkdir('models')
    
        ## 检查GPU是否可用，如果是使用GPU，否使用CPU
        use_gpu = torch.cuda.is_available()
        if use_gpu:
            model = model.cuda()
    
        ## 创建数据预处理函数，训练预处理包括随机裁剪缩放、随机翻转、标准化，验证预处理包括中心裁剪，标准化
        data_transforms = {
            'train': transforms.Compose([
                transforms.RandomResizedCrop(crop_size),
                transforms.RandomHorizontalFlip(),
                transforms.RandomRotation(10),
                transforms.ColorJitter(brightness=0.1,contrast=0.1,saturation=0.1,hue=0.1),
                transforms.ToTensor(),
                transforms.Normalize([0.5,0.5,0.5], [0.5,0.5,0.5])
            ]),
            'val': transforms.Compose([
                transforms.Resize(image_size),
                transforms.CenterCrop(crop_size),
                transforms.ToTensor(),
                transforms.Normalize([0.5,0.5,0.5], [0.5,0.5,0.5])
            ]),
        }
    
        ## 读取数据
        cls_labels_dict = np.load("cls_labels.npy", allow_pickle=True).item()
        trainlist = 'VOC2012/ImageSets/Main/train.txt'
        vallist = 'VOC2012/ImageSets/Main/val.txt'
        dataroot = 'VOC2012'
        train_datalist = load_img_name_list(trainlist)
        trainlabellist = load_image_label_list_from_npy(cls_labels_dict,train_datalist)
        val_datalist = load_img_name_list(vallist)
        vallabellist = load_image_label_list_from_npy(cls_labels_dict,val_datalist)
    
        ## 自己定义的数据类
        traindataset = VOCDataset(datalist=train_datalist, labellist=trainlabellist, data_root=dataroot,
                            data_transform=data_transforms['train'])
        valdataset = VOCDataset(datalist=val_datalist, labellist=vallabellist, data_root=dataroot,
                            data_transform=data_transforms['val'])
    
        image_datasets = {}
        image_datasets['train'] = traindataset
        image_datasets['val'] = valdataset
    
        ## 创建数据指针，设置batch大小，shuffle，多进程数量
        dataloaders = {x: torch.utils.data.DataLoader(image_datasets[x],
                                                     batch_size=64,
                                                     shuffle=True,
                                                     num_workers=4) for x in ['train', 'val']}
    
        ## 优化目标使用多标签损失，优化方法使用带动量项的SGD，学习率迭代策略为step，每隔100个epoch，变为原来的0.1倍
        optimizer_ft = optim.SGD(model.parameters(), lr=0.1, momentum=0.9)
        step_lr_scheduler = lr_scheduler.StepLR(optimizer_ft, step_size=100, gamma=0.1)
    
        model = train_model(model=model,
                               optimizer=optimizer_ft,
                               scheduler=step_lr_scheduler,
                               num_epochs=300)
    
        torch.save(model.state_dict(),'models/model.pt')
    

### 模型测试

  * 平均精度(mAP)，基于PR曲线进行计算


![img_36.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_36.png)

  * 训练集与验证集mAP指标


![img_37.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/07图像分类技术与项目实战/img_37.png)
    
    
    如何进一步提高指标?
    (1)使用预训练模型
    (2)增加训练数据量
    (3)使用更小的模型
    (4)使用更多数据增强方法
    (5)....
    

  * multi_label/inference.py


    
    
    #coding:utf8
    
    # Copyright 2024 longpeng2008. All Rights Reserved.
    # Licensed under the Apache License, Version 2.0 (the "License");
    # If you find any problem,please contact us
    #
    #     longpeng2008to2012@gmail.com 
    #
    # or create issues
    # =============================================================================
    import sys
    import numpy as np
    import cv2
    import os
    import dlib
    
    import torch
    from torchvision import datasets, models, transforms
    import time
    from PIL import Image
    import torch.nn.functional as F
    
    from net import ResNet18
    
    ## 全局变量
    testsize = 224 ##测试图大小
    nclass = 20  ##分类类别数
    net = ResNet18(num_classes=nclass)  ##创建模型
    net.eval() ## 设置推理模式，使得dropout和batchnorm等网络层在train和val模式间切换
    torch.no_grad() ## 停止autograd模块的工作，以起到加速和节省显存
    # 标签字符串名称
    CAT_LIST = ['aeroplane', 'bicycle', 'bird', 'boat',
            'bottle', 'bus', 'car', 'cat', 'chair',
            'cow', 'diningtable', 'dog', 'horse',
            'motorbike', 'person', 'pottedplant',
            'sheep', 'sofa', 'train',
            'tvmonitor']
    
    ## 载入模型权重
    modelpath = 'models/model.pt'
    net.load_state_dict(torch.load(modelpath,map_location=lambda storage,loc: storage))
    
    ## 定义预处理函数
    data_transforms = transforms.Compose([
                transforms.ToTensor(),
                transforms.Normalize([0.5,0.5,0.5], [0.5,0.5,0.5])])
    
    #一次测试一个文件
    imagedir = 'VOC2012/JPEGImages'
    imagepaths = os.listdir(imagedir)
    for imagepath in imagepaths:
        im = cv2.imread(os.path.join(imagedir,imagepath),1)
        im_ = cv2.cvtColor(im,cv2.COLOR_BGR2RGB) ## 从BGR到RGB格式转换，保证OpenCV和PIL读取一致性
        im_ = cv2.resize(im_,(testsize,testsize)) ## 图像缩放
        imgblob = data_transforms(im_).unsqueeze(0) ## 数据预处理
        imgblob.requires_grad = False
        predict = np.squeeze(F.sigmoid(net(imgblob)).detach().numpy()) ## 获得原始预测结果
        print('predict=',predict)
        sorted_index = np.argsort(predict)
        print('sorted index=',sorted_index)
    
        ## 将结果绘制到图片上
        im_h,im_w,im_c = im.shape
        pos_x = int(im_w/5)
        font = cv2.FONT_HERSHEY_SIMPLEX
        cv2.putText(im, CAT_LIST[int(sorted_index[-1])]+':'+str(predict[int(sorted_index[-1])]), (pos_x,int(im_h/3)), font, 0.8, (0,0,255), 2)
        cv2.putText(im, CAT_LIST[int(sorted_index[-2])]+':'+str(predict[int(sorted_index[-2])]), (pos_x,int(im_h/2)), font, 0.8, (0,255,0), 2)
        cv2.putText(im, CAT_LIST[int(sorted_index[-3])]+':'+str(predict[int(sorted_index[-3])]), (pos_x,int(2*im_h/3)), font, 0.8, (255,0,0), 2)
        cv2.namedWindow('result',0)
        cv2.imshow('result',im)
        cv2.imwrite(os.path.join('results',imagepath),im)
        k = cv2.waitKey(0)
        if k == ord('q'):
            quit(0)