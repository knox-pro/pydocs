# MobileNet系列

## MobileNet V1

  * Google提出的移动端高效率模型
  * 相比VGG16，准确度减少0.9%，模型参数只有VGG的1/32


![img_12.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_12.png)

### MobilenetV1核心模块

  * 通道可分离卷积:将标准的卷积分解为两步


#### 第一步 ： DepthWise Convolution多通道卷积

> 每个通道独立进行卷积运算，卷积核的数量与上一层的通道数相同，计算量相比于标准卷积降低了1个数量级，但没有有效利用不同通道在相同空间位置上的信息，

![img_13.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_13.png)

#### 第二步： PointWise Convolution点卷积

> 标准多通道卷积，卷积核的尺寸为1x1xMxN,M为输入特征通道数， N为输出特征通道数。

![img_14.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_14.png)

  * 常规卷积(左)与深度可分离卷积(右)模块


![img_15.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_15.png)

### MobileNet V1 整体结构

> DepthWise Convolution简写为dw; PointWise Convolution简写为sl;

![img_16.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_16.png)

> 1X1卷积上占据了绝大部分的参数和运算量

![img_17.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_17.png)

  * 模型参数量和计算量压缩比较
  * 计算量约为原来的 1/10


![img_19.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_19.png)

![img_18.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_18.png)

  * 两个超参数控制MobileNet大小
  * Width multiplier 系数a:按比例减少通道数，取值（0,1]
  * Resolution multiplier 系数p:按比例降低特征图的大小,取值（0,1]


![img_20.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_20.png) Mult-Adds乘加计算量，Parameters参数量，对比Convolution和DepthWise Separable Conv, 相差一个量级，同个超参数看，可以进一步降低计算量到15.1和参数量到0.15。

### MobileNet V1实验

  * 与标准卷积比较，精度下降了一点，71.7%下降到70.6%。
  * 与更窄和更浅比较，均有下降，更窄的下降为68.4%，更浅的下降为65.3%
  * 结论：不要更浅，模型深一些更好，通道数可以少一点，窄一些影响不大。


![img_21.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_21.png)

![img_22.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_22.png)

  * 与其他模型的对比


> ImageNet数据集的对比

![img_24.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_24.png)

> Stanford Dogs数据集的对比

![img_23.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_23.png)

## MobileNet V2

### MobileNet V1的问题

  * 使用ReLU后会造成信息丢失，通道少的特征图尤其明显
  * 当输出维度m=2,3时，信息坍塌，当m>15时，信息基本被保留


![img_25.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_25.png)

### MobileNet V2 原理

  * 整体采用Mobilenet V1的通道可分离卷积来降低运算量
  * 采用ResNet思想，引入了特征复用结构，缓解特征退化问题(低精度训练尤其有效)
  * 采用Inverted residual block结构，回避ReLU缺陷，通道数比较少的瓶颈层不采用非线性激活函数。


#### 反转残差模块（Inverted residual block）

![img_27.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_27.png)

（a）标准残差模块： 先降维再升维 （b) 反转残差模块: 先升维再降维

> 传统残差结构先用1x1的卷积降低通道数，然后进行卷积操作，最后用1x1卷积将通道数恢复到原始大小。跳层连接建立在两个通道数比较多的层之间，且每层都采用 ReLU 激活
> 
> 反转残差结构先用1x1卷积提高通道数，然后进行深度卷积操作，最后用1x1卷积将通道数降至原始大小。跳层连接建立在两个通道数比较少的瓶颈层之间，且画阴影的两个块没有ReLU 激活函数。

#### 扩展因子

  * 扩展因子t: 瓶颈层到中间层通道数的提升比例
  * RELU6： 限制激活的最大值为6


![img_28.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_28.png)

  * 首先通过1X1的conv2d卷积接上ReLu6激活函数，将h x w x k的输入特征图转换为h x w x tk的输出，这里的tk是基于拓展因子t变成的tk;
  * 接下来用3 x 3的分组卷积，降低空间分辨率，这里有个参数s；
  * 然后把通道数tk变成更小的通道数k',使用的线性1 X 1 卷积（后面不接激活函数）。


![img_29.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_29.png) stride=1有个跳层， stride=2不做跳层，上述表格反转残差模块三个步骤。

### 模型结构配置

  * 第一层是标准卷积，后面是前述的瓶颈结构。其中t是扩展因子，c是输出通道数，n是重复次数，s代表步长。


![img_30.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_30.png)

### mobileNet V2 实验

与其他模型比较，mobilenetV2拥有更少的参数量，更高的精度 ![img_31.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_31.png)

## 基于Pytorch搭建MobileNet

  * Mobilenet V1结构


![img_32.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_32.png)

  * Mobilenet V2结构


![img_33.png](http://cyc.chenyangstudy.cn/book_chenyang/a05深度学习与机器视觉/03移动端AI高效率分组模型/img_33.png)

### MobileNet V1实现
    
    
    #coding:utf8
    import time 
    import torch
    import torch.nn as nn
    import torchvision.models as models
    
    class MobileNetV1(nn.Module):
        def __init__(self):
            super(MobileNetV1,self).__init__()
    
            # 标准卷积
            def conv_bn(inp,oup,stride):
                return nn.Sequential(
                        nn.Conv2d(inp,oup,3,stride,1,bias = False),
                        nn.BatchNorm2d(oup),
                        nn.ReLU(inplace = True))
    
            # 深度可分离卷积，depthwise convolution + pointwise convolution
            def conv_dw(inp,oup,stride):
                return nn.Sequential(
                        nn.Conv2d(inp,inp,3,stride,1,groups = inp,bias = False),
                        nn.BatchNorm2d(inp),
                        nn.ReLU(inplace = True),
    
                        nn.Conv2d(inp,oup,1,1,0,bias = False),
                        nn.BatchNorm2d(oup),
                        nn.ReLU(inplace = True))
    
            #网络模型声明
            self.model = nn.Sequential(
                    conv_bn(3,32,2),
                    conv_dw(32,64,1),
                    conv_dw(64,128,2),
                    conv_dw(128,128,1),
                    conv_dw(128,256,2),
                    conv_dw(256,256,1),
                    conv_dw(256,512,2),
                    conv_dw(512,512,1),
                    conv_dw(512,512,1),
                    conv_dw(512,512,1),
                    conv_dw(512,512,1),
                    conv_dw(512,512,1),
                    conv_dw(512,1024,2),
                    conv_dw(1024,1024,1),
                    nn.AvgPool2d(7),)
    
            self.fc = nn.Linear(1024,1000)
    
        #网络的前向过程    
        def forward(self,x):
            x = self.model(x)
            x = x.view(-1,1024)
            x = self.fc(x)
            return x
    
    #速度评估
    def speed(model,name):
        t0 = time.time()
        input = torch.rand(1,3,224,224).cpu()
        t1 = time.time()
    
        model(input)
        t2 = time.time()
    
        for i in range(0,30):
            model(input)
        t3 = time.time()
    
        print('%10s : %f'%(name,(t3 - t2)/30))
    
    if __name__ == '__main__':
        resnet18 = models.resnet18().cpu()
        alexnet = models.alexnet().cpu()
        vgg16 = models.vgg16().cpu()
        mobilenetv1 = MobileNetV1().cpu()
    
        speed(resnet18,'resnet18')
        speed(alexnet,'alexnet')
        speed(vgg16,'vgg16')
        speed(mobilenetv1,'mobilenet')
    

### MobileNetV2实现
    
    
    #coding:utf8
    import time
    import torch
    import torch.nn as nn
    
    # 调整通道数量，使其都可以除8
    def _make_divisible(v, divisor, min_value=None):
        if min_value is None:
            min_value = divisor
        new_v = max(min_value, int(v + divisor / 2) // divisor * divisor)
        # 保证向下取整损失不要超过10%.
        if new_v < 0.9 * v:
            new_v += divisor
        return new_v
    
    # 标准卷积模块
    class ConvBNReLU(nn.Sequential):
        def __init__(self, in_planes, out_planes, kernel_size=3, stride=1, groups=1):
            padding = (kernel_size - 1) // 2
            super(ConvBNReLU, self).__init__(
                nn.Conv2d(in_planes, out_planes, kernel_size, stride, padding, groups=groups, bias=False),
                nn.BatchNorm2d(out_planes),
                nn.ReLU6(inplace=True)
            )
    
    # 反转残差模块
    class InvertedResidual(nn.Module):
        def __init__(self, inp, oup, stride, expand_ratio):
            super(InvertedResidual, self).__init__()
            self.stride = stride
            assert stride in [1, 2]
    
            hidden_dim = int(round(inp * expand_ratio))
            self.use_res_connect = self.stride == 1 and inp == oup
    
            layers = []
            if expand_ratio != 1:
                # pw
                layers.append(ConvBNReLU(inp, hidden_dim, kernel_size=1))
            layers.extend([
                # dw
                ConvBNReLU(hidden_dim, hidden_dim, stride=stride, groups=hidden_dim),
                # pw-linear
                nn.Conv2d(hidden_dim, oup, 1, 1, 0, bias=False),
                nn.BatchNorm2d(oup),
            ])
            self.conv = nn.Sequential(*layers)
    
        def forward(self, x):
            if self.use_res_connect:
                return x + self.conv(x)
            else:
                return self.conv(x)
    
    # 模型定义模块
    class MobileNetV2(nn.Module):
        def __init__(self, num_classes=1000, width_mult=1.0, inverted_residual_setting=None, round_nearest=8):
            super(MobileNetV2, self).__init__()
            block = InvertedResidual
            input_channel = 32
            last_channel = 1280
    
            if inverted_residual_setting is None:
                inverted_residual_setting = [
                    # t, c, n, s
                    [1, 16, 1, 1],
                    [6, 24, 2, 2],
                    [6, 32, 3, 2],
                    [6, 64, 4, 2],
                    [6, 96, 3, 1],
                    [6, 160, 3, 2],
                    [6, 320, 1, 1],
                ]
    
            # 需要输入t,c,n,s参数
            if len(inverted_residual_setting) == 0 or len(inverted_residual_setting[0]) != 4:
                raise ValueError("inverted_residual_setting should be non-empty "
                                 "or a 4-element list, got {}".format(inverted_residual_setting))
    
            # 第一层
            input_channel = _make_divisible(input_channel * width_mult, round_nearest)
            self.last_channel = _make_divisible(last_channel * max(1.0, width_mult), round_nearest)
            features = [ConvBNReLU(3, input_channel, stride=2)]
            # inverted residual blocks
            for t, c, n, s in inverted_residual_setting:
                output_channel = _make_divisible(c * width_mult, round_nearest)
                for i in range(n):
                    stride = s if i == 0 else 1
                    features.append(block(input_channel, output_channel, stride, expand_ratio=t))
                    input_channel = output_channel
            # 最后几层
            features.append(ConvBNReLU(input_channel, self.last_channel, kernel_size=1))
            # make it nn.Sequential
            self.features = nn.Sequential(*features)
    
            # building classifier
            self.classifier = nn.Sequential(
                nn.Dropout(0.2),
                nn.Linear(self.last_channel, num_classes),
            )
    
        def forward(self, x):
            x = self.features(x) # 7*7*1280
            x = x.mean([2, 3])
            x = self.classifier(x)
            return x
    
    # 速度评估
    def speed(model, name):
        t0 = time.time()
        input = torch.rand(1, 3, 224, 224).cpu()
        t1 = time.time()
    
        model(input)
        t2 = time.time()
    
        for i in range(0, 30):
            model(input)
        t3 = time.time()
    
        print('%10s : %f' % (name, (t3 - t2) / 30))
    
    if __name__ == '__main__':
        from mobilenetv1 import MobileNetV1
        mobilenetv1 = MobileNetV1().cpu()
        mobilenetv2 = MobileNetV2(width_mult=1)
        speed(mobilenetv1, 'mobilenetv1')
        speed(mobilenetv2, 'mobilenetv2')
    
        input = torch.randn([1, 3, 224, 224])
        output = mobilenetv1(input)
        torch.onnx.export(mobilenetv1,input,'mobilenetv1.onnx')
    
        output = mobilenetv2(input)
        torch.onnx.export(mobilenetv2,input,'mobilenetv2.onnx')